Unit тесты + баг репорты для решения задачи с leetcode лежат в папке backend/unit-tests
Само решение расположено в папке backend под названием regularExpressionMatching.js

Как запустить бэкенд часть?

    1. Перейти в папку backend:
        cd backend
    2. Установить модули:
        npm i
    3. Запустить сервер на порту 5000 командой:
        npm start
    4. Чтобы запустить unit тесты, прописать команду:
        npm run test

Как запустить фронтенд часть?

    1. Перейти в папку frontend:
        cd frontend
    2. Установить модули:
        npm i
    3. Запустить сервер на порту 3000 командой:
        npm start
